name: Build
on:
  # push:
  #   tags:
  #     - "*"
  #   branches:
  #     - "main"
  release:
    types: [published]

jobs:
  build:
    runs-on: default
    steps:
    # Setup
    - uses: actions/checkout@v6
    - uses: actions/setup-go@v6
      with:
        go-version: "1.26" 

    # Build
    - run: |
        mkdir bin
        GOOS=linux GOARCH=amd64 go build -o bin/kubegen_$${{ forge.ref_name }}_linux ./cmd/kubegen
        GOOS=windows GOARCH=amd64 go build -o bin/kubegen_$${{ forge.ref_name }}_win.exe ./cmd/kubegen
        GOOS=darwin GOARCH=arm64 go build -o bin/kubegen_$${{ forge.ref_name }}_mac ./cmd/kubegen

    # Publish
    - uses: actions/forgejo-release@v2
      with:
        direction: upload
        url: http://forgejo
        repo: rosco/kubegen
        token: ${{ secrets.app_token }}
        override: true
        release-dir: bin
    - uses: actions/forgejo-release@v2
      with:
        direction: upload
        url: http://forgejo
        repo: rosco/kubegen
        token: ${{ secrets.app_token }}
        override: true
        release-dir: bin
        tag: latest
